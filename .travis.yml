language: python
python:
  - '3.4'
  - '3.5'
  - '3.6'
  - 'nightly'

cache: pip

install:
  - pip install .
  - pip install pylint black pytest

script:
  - pylint ./kieli.py ./test_kieli.py
  - black --check .
  - pytest -vv

notifications:
  irc:
    channels:
      - 'chat.freenode.net#8Banana'
    skip_join: true
    on_success: change
    on_failure: always
    template:
      - '%{repository}#%{build_number} (%{branch} - %{commit} : %{author}): %{message} %{build_url}'

matrix:
  allow_failures:
    - python: 'nightly'
